---
interface Props {
  href?: string;
  className?: string;
}

const { href = "#", className = "" } = Astro.props;
---

<a 
  href={href} 
  class={`text-[#2d3748] hover:text-[#29b6f6] font-medium transition-colors text-[15px] ${className}`}
>
  <slot />
</a>
